// **************************************************************************
// RealmObjectGenerator
// **************************************************************************

class Person extends _Person with RealmEntity, RealmObject {
  Person({
    Person? spouse,
  }) {
    _spouseProperty.setValue(this, spouse);
  }

  Person._();

  static const _spouseProperty = ObjectProperty<Person>('spouse');
  @override
  Person? get spouse => _spouseProperty.getValue(this);
  @override
  set spouse(covariant Person? value) => _spouseProperty.setValue(this, value);

  @override
  Stream<RealmObjectChanges<Person>> get changes =>
      RealmObject.getChanges(this);

  static const schema = SchemaObject<Person>(
    Person._,
    'Person',
    {
      'spouse': _spouseProperty,
    },
  );
  @override
  Map<String, ValueProperty> get properties => schema.properties;
}
