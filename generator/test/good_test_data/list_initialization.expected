// **************************************************************************
// RealmObjectGenerator
// **************************************************************************

class Person extends _Person with RealmEntity, RealmObject {
  Person({
    Iterable<Person> children = const [],
  }) {
    _childrenProperty.setValue(this, RealmList<Person>(children));
  }

  Person._();

  static const _childrenProperty =
      ListProperty<Person>('children', RealmPropertyType.object);
  @override
  RealmList<Person> get children => _childrenProperty.getValue(this);
  @override
  set children(covariant RealmList<Person> value) =>
      throw RealmUnsupportedSetError();

  @override
  Stream<RealmObjectChanges<Person>> get changes =>
      RealmObject.getChanges(this);

  static const schema = SchemaObject<Person>(
    Person._,
    'Person',
    {
      'children': _childrenProperty,
    },
  );
  @override
  Map<String, ValueProperty> get properties => schema.properties;
}
