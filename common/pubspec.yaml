name: realm_common
description: Hosts the common code shared between realm, realm_dart and realm_generator packages.
             This package is part of the official Realm Flutter and Realm Dart SDKs.
             Realm is a mobile database - an alternative to SQLite and key-value stores.
version: 0.2.0-alpha

environment:
  sdk: '>=2.15.0 <3.0.0'

dev_dependencies:
  lints: ^1.0.1
