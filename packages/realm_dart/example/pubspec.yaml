name: myapp
description: A simple command-line application using Realm Dart SDK.

publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  ejson: ^0.1.0
  realm_dart:
    path: ../

dev_dependencies:
  lints: ^3.0.0
